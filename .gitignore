# Local development configs (contains secrets)
local/
.env

# Docker mounted volume (user config, credentials, session data)
config/
workspace/

# Claude Code local settings
.claude/
docker-compose.override.yml
